# Wireless-Controller

This project demonstrates using a Arduino IOT 33 dev board to stream IMU data to a computer.
The computer reads this data and runs it through a random forest model to detect the gesture of the user. 

The current gestures are: idle, shake, jab, uppercut, and hook

### Wireless Pipeline
To pass in the data the Arduino is talking to the computer over wifi using...

### Training the Model

